services:
  - type: web
    name: storyverse-backend
    env: node
    rootDir: MMA_BE
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
